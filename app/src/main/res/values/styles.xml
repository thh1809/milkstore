<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Layout cha với nền nâu nhạt -->
    <style name="AdminBackground">
        <item name="android:background">#D7CCC8</item> <!-- Nâu nhạt -->
        <item name="android:padding">16dp</item>
        <item name="android:orientation">vertical</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <!-- Button back với nền nâu + chữ đen -->
    <style name="AdminButton">
        <item name="android:backgroundTint">#A1887F</item>
        <item name="android:textColor">#000000</item>
        <item name="android:padding">12dp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_gravity">start</item>
        <item name="android:layout_marginBottom">16dp</item>
    </style>

    <!-- Style cho ProgressBar (nếu cần) -->
    <style name="AdminProgressBar">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_gravity">center</item>
    </style>

    <!-- Style cho RecyclerView -->
    <style name="AdminRecyclerView">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:padding">8dp</item>
    </style>

    <!-- Style cho FloatingActionButton -->
    <style name="AdminFab">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_gravity">end|bottom</item>
        <item name="android:layout_margin">16dp</item>
    </style>

    <style name="MilkButton" parent="Widget.AppCompat.Button">
        <item name="android:backgroundTint">#D2B48C</item> <!-- Nâu nhạt -->
        <item name="android:textColor">#000000</item> <!-- Đen -->
        <item name="android:fontFamily">sans-serif-medium</item> <!-- Tùy chọn -->
        <item name="android:padding">8dp</item>
        <item name="android:layout_margin">4dp</item>
    </style>



</resources>
